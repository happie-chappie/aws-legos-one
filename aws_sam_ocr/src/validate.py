# def validate(df, string_columns):
def validate(df, string_columns):
    """
    This method returns true if all the string columns in the
    dataframe are of type string and rest of the columns are
    of type 
    """
